# Redis Cluster 개요
## Redis Cluster 목표

## 구현된 Subset
---
Redis Cluster는 해시태그라는 개념이 있어, 특정 키를 동일한 해시 슬롯에 강제로 저장할 수 있다. 하지만 수동으로 재샤딩 중에는 멀티 키 연산은 단일 키 연산이 가능한 동안 사용할 수 없다.

Redis Cluster는 Standalone 버전의 Redis처럼 다수의 데이터베이스를 지원하지 않는다.(데이터베이스0만 지원) 그리고 SELECT 명령도 허용하지 않는다.

## Redis Cluster 프로토콜에서 클라이언트, 서버 역할
---
Redis Cluster에서 노드는 데이터를 보관하고 올바른 노드에 키를 매핑하는 것을 포함하여 클러스터의 상태를 파악하는 역할을 한다. 클러스터 노드는 장애가 발생했을 때 계속 작동하기 위해 필요할 때 다른 노드를 자동으로 검색하고 작동하지 않는 노드를 감지하며 복제 노드를 마스터하도록 승격할 수도 있다.

모든 클러터 노드는 Redis Cluster Bus로 불리는 TCP버스와 binary 프로토콜을 사용해 서로 연결한다. 노드는 가십(gossip) 프로토콜을 사용하여 클러스터에 대한 정보를 전파하여 새로운 노드를 발견하고, ping 패킷을 전송하여 다른 모든 노드가 제대로 작동하는지 확인하며, 특정 조건을 알리는 데 필요한 클러스터 메시지를 보냅니다. 클러스터 버스는 또한 클러스터 전체에 Pub/Sub 메시지를 전파하고 사용자가 요청할 때 수동 failover를 조정하는 데 사용된다.(수동 페일오버는 레디스 클러스터 장애 감지기가 시작하지 않고 시스템 관리자가 직접 실행하는 failover).

클러스터 노드는 요청을 프록시할 수 없기 때문에 클라이언트를 리디렉션 오류 -MOVED 및 -ASK를 사용하여 다른 노드로 리디렉션할 수 있다. 클라이언트는 이론적으로 클러스터의 모든 노드에 요청을 자유롭게 보낼 수 있고, 필요에 따라 리디렉션되므로 클라이언트가 클러스터의 상태를 유지할 필요가 없다. 그러나 키와 노드 사이의 맵을 캐시할 수 있는 클라이언트는 합리적인 방법으로 성능을 향상시킬 수 있다.

## Write Safety
---
Redis Cluster는 노드 간 비동기 복제를 사용하며, 마지막 failover는 merge함수를 수행한다. 마지막으로 선택된 마스터 데이터 세트가 결국 다른 모든 복제본을 대체한다는 것을 의미한다. 파티션 중 쓰기가 손실될 수 있는 window of time은 항상 존재한다. 하지만 이 window of time은 클라이언트가 다수의 마스터와 연결된 경우와 소수의 마스터와 연결된 경우에 따라 다르다.

Redis Cluster는 클라이언트가 소수의 마스터와 연결된 경우보다 다수의 마스터와 연결된 경우에 쓰기 작업을 보장하기 위해 노력한다. 아래는 다수의 파티션에서 쓰기를 실패하는 시나리오를 보여눈다.

1. 쓰기 작업이 유효한 타임아웃 내에 마스터에 작업완료가 될 수 있으나, 비동기 복제를 통한 replica로의 복제가 정상적으로 수행되지 않을 수 있다. 만약 쓰기 작업 중 마스터가 shutdown되면 replica중 하나가 master로 승격되는 시간동안 마스터에 접근할 수 없는 경우(?) 해당 쓰기 작업은 영구적으로 손실된다. 마스터들은 거의 동시에 쓰기작업과 복제하기 때문에 이러한 장애를 보기는 힘들다.

2. 쓰기 실패가 발생하는 다른 시나리오를 살펴보면, 
* 파티션 문제로 마스터에 연결할 수 없다. 
* 복제가 replica중 하나에 실패한다.
* 시간이 지나서 다시 마스터에 연결 가능하다.
* 예전 라우팅테이블 정보를 가진 클라이언트는 새롭게 마스터가된 replica가 아닌, 이전 마스터에 쓰기 작업을 시도한다.

failover되는 시간동안 대다수의 다른 마스터 노드와 통신할 수 없는 마스터 노드는 쓰기 작업을 허용하지 않으며, 파티션이 고정된 경우 다른 노드들이 구성 변경사항을 알 수 있도록 해당 시간동안 쓰기 작업을 거부하기 때문에 위 실패 사례도 발생할 가능성이 낮다. 이 실폐 사례도 클라이언트의 라우팅 테이블이 아직 업데이트 되지 않은 상태이다.

파티션의 일부분(소수)을 대상으로하는 쓰기 작업은 window of time이 더 큽니다. 예를 들어, Redis Cluster는 소수의 마스터와 하나 이상의 클라이언트가 있는 파티션에서 쓰기 작업을 실패할 수 있는데, 마스터에게 전달되는 모든 쓰기 작업이 다수의 마스터에게 실패할 수 있다.

특히 마스터가 실패하려면 NODE_TIMEOUT 시간동안 다른 마스터와 연결할 수 없어야 하므로 해당 시간 이전에 파티션이 고정되면 쓰기 손실이 발생하지 않는다. 파티션이 NODE_TIMEOUT시간보다 오래 지속되면, 해당 시점까지 소수 마스터에서 수행한 쓰기 작업이 모두 취소될 수 있다. Redis Cluster의 소수 측은 다수의 마스터와 컨택하지 못한 시간이 NODE_TIMEOUT 만큼 지나면 쓰기 작업을 거절한다. 그래서 그 이후로 소수 측을 더이상 사용하지 못하는 최대 window가 있다. 따라서 해당 시간 이후에는 쓰기가 허용되거나 손실되지 않습니다.

## Availability
Redis Cluster는 파티션의 소수 부분에서는 사용할 수 없다. 파티션의 다수 부분에서 다수의 마스터가 있고 연결할 수 없는 모든 마스터에 대한 replica가 있다고 가정할 때, 클러스터는 NODE_TIMEOUT 시간에서 replica가 master를 선출하는 시간을 더한 만큼이 지난 후에 다시 사용 가능하다. (페일오버는 보통 1~2초 소요된다)
즉, Redis Cluster는 클러스터 내 몇 개 노드의 장애를 극복하도록 설계되었지만, 대규모 네트워크 분할 시 가용성을 요구하는 애플리케이션에는 적합한 솔루션이 아니다.
예를들어 N개의 마스터 노드가 있고, 각 노드당 하나의 replica로 구성된 클러스터에서 클러스터의 대부분은 단일 노드가 분할되어 있는 한 계속 사용할 수 있으며 두 노드가 분할되어 있을 때 1-(1/(N*2-1)) 확률로 계속 사용할 수 있다. 예를 들어, 노드당 5개의 노드가 있고 단일 복제본이 있는 클러스터에서는 대다수에서 두 개의 노드가 분할된 후 클러스터를 더 이상 사용할 수 없게 될 확률이 1/(5*2-1) = 11.11%이다. replica 마이그레이션이라고 불리는 Redis Cluster 기능 덕분에 많은 실제 시나리오에서 replica가 고아 마스터(마스터가 더 이상 복제본을 가지고 있지 않음)로 마이그레이션됨으로써 클러스터 가용성이 향상된다. 따라서 성공적인 실패 이벤트마다 클러스터는 다음 실패에 더 잘 저항하기 위해 복제본 레이아웃을 재구성할 수 있다.

## 데이터 저장 메커니즘
---
Redis Cluster는 총 16384개(0~16383)의 슬롯을 가지고 있다. 이 슬롯의 개수는 Master를 몇대로 구성하던 변하지 않는다. Redis는 키의 CRC16 해시 값을 계산하고, 이를 16384로 나눈 나머지를 사용해 슬롯 번호를 결정한다.   
계산식 : slot = CRC16(key) % 16384  
lettuce와 같은 클라이언트 라이브러리를 사용해 데이터를 저장할 경우에는 클라이언트에서 slot을 계산하고, 클라이언트는 CLUSTER SLOTS 명령으로 클러스터맵을 가지고 있어 알맞는 노드(마스터)에 데이터를 저장한다. 만약 redis-cli를 통해 직접 set 명령어를 수행할 경우 접속한 redis에 해당 슬롯이 없으면 저장이 되지 않는다. 즉, 클라이언트가 직접 다시 올바른 노드에 저장을 해야한다. 
예를 들어서 클라이언트로 데이터를 저장했는데 해당 노드에 슬롯이 없는 경우 MOVED 응답을 받고, 올바를 노드에 다시 set을 진행한다. 

## 데이터 동기화 메커니즘
---

클러스터 동기화 메커니즘은 master-slave 메커니즘과 동일하다. 차이점은 아래와 같다.   
1. 슬롯 기반 데이터 동기화
- 클러스터 환경에서는 마스터가 담당하는 슬롯의 데이터만 슬레이브로 동기화
2. 클러스터 메타데이터 동기화 
  - 마스터-슬레이브 동기화 과정에서 데이터뿐만 아니라, 클러스터 노드 정보 및 슬롯 매핑 정보도 동기화

## Redis Cluster Bus  
---

Redis Cluster Bus는 Redis 클러스터 노드 간의 상태 관리 및 통신을 위한 전용 통신 채널이다. 클러스터 상태를 유지하고 노드 간의 동기화를 수행하며, 장애 감지와 슬롯 재배치와 같은 중요한 작업을 처리한다. Redis의 Cluster Bus는 클라이언트와의 일반적인 데이터 통신과는 별도로 동작한다.   

1.Cluster Bus의 주요 역할  
1.1 노드 상태 관리  
클러스터 내의 모든 노드는 주기적으로 서로 PING 메시지를 전송하여 상태를 확인합니다.
응답(PONG 메시지)을 통해 다른 노드가 정상인지 확인합니다.
일정 시간 동안 응답이 없으면 해당 노드를 PFAIL(Partial Failure) 상태로 간주합니다.
다수의 노드가 노드를 FAIL 상태로 간주하면 클러스터 전체에 이 정보를 전파합니다.  

1.2 슬롯 및 노드 정보 동기화
클러스터 노드 간의 슬롯 분배 상태와 노드 매핑 정보를 교환합니다.
새로운 노드가 클러스터에 추가되거나 슬롯이 재배치될 경우 이 정보를 다른 노드에 전달합니다.  

1.3 장애 감지 및 Failover  
마스터 노드가 장애 상태에 빠지면 슬레이브 노드가 이를 감지하고 Failover를 수행합니다.
승격된 슬레이브가 새로운 마스터로 설정되었음을 모든 노드에 알립니다.  

1.4 클러스터 재구성
클러스터의 재구성(예: 슬롯 이동, 노드 추가/삭제) 시 관련 정보를 전파하여 클러스터 상태를 일관성 있게 유지합니다.

2.Cluster Bus의 특징  

2.1 통신 포트
Redis 노드의 기본 포트가 6379인 경우, Cluster Bus는 기본 포트에서 +10000된 포트(16379)를 사용합니다.
이 포트는 클러스터 내부의 노드 간 통신에만 사용됩니다.  

2.2 통신 프로토콜
Cluster Bus는 Redis의 일반 클라이언트 통신과는 별도로 전용 바이너리 프로토콜을 사용하여 동작합니다.
이 프로토콜은 효율적이고 가벼운 메시지 교환에 최적화되어 있습니다.  

2.3 Full Mesh 통신
모든 노드는 Cluster Bus를 통해 다른 모든 노드와 연결된 Full Mesh 구조로 통신합니다.
이는 빠른 장애 감지와 상태 전파를 가능하게 합니다.


<b>gossip message란?</b>   
클러스터의 노드 간 상태 정보를 주고받기 위해 사용하는 메시지. 장애 상태(PFAIL, FAIL)와 같은 중요한 정보를 빠르게 공유하고 새로운 노드가 추가되거나 노드의 구성 변경 사항(예: 슬롯 이동)을 전파한다.

<b>Gossip 섹션이란?</b>   
Gossip 메시지 내에 포함된 상태 정보의 개별 항목.   
노드 ID,노드 IP 주소 및 포트,노드 상태 (PFAIL, FAIL 등),최근 핑 응답 시간

## 클러스터 노드 상태 체크 및 승격 메커니즘
---
클러스터 노드의 상태는 pfail과 fail이 존재한다. pfail(possible fail) 잠재적인 fail로 단일 노드가 특정 노드(장애로 예상되는)와의 ping, pong이 실패한 경우 그 특정 노드를 pfail로 정한다. 이 상태에서 다른 노드들도 장애로 예상되는 노드를 pfail로 결정할 경우 fail로 변경한다. 슬레이브 노드가 마스터로 승격되는 트리거가 된다.  



## 클러스터 관리 메커니즘(슬롯, 재배치 등)
---
클러스터를 관리하는 파일이 nodes.conf 존재한다. 이 파일은 노드(redis)들이 작성하기때문에
임의로 수정 및 삭제하면 안된다. 해당 위치는 redis.conf에서 수정가능하다. 


## 소스 분석
### server.c

클러스터 모드에서 주기적으로 실행되는 작업을 처리하는 핵심 함수이다.

```c
   /* Run the Redis Cluster cron. */
    run_with_period(100) {
        if (server.cluster_enabled) clusterCron();
    }
``` 
주요역할  
1. 클러스터 상태 업데이트  
  - 슬롯 상태 점검, 노드 상태 업데이트  

2. 노드 간 핑/퐁 메시지 교환  
  - Ping-Pong 프로토콜을 처리하여, 클러스터 내에서 각 노드의 상태를 주기적으로 확인
    ping은 현재 노드가 다른 노드에 상태를 확인하기 위해 보냄, pong은 응답 메시지로, 노드가 정상 동작 중임을 알림  

3. 슬롯 이동 처리
 - 데이터 재분배(resharding)나 클러스터 재구성 중 **슬롯 이동(migrating/importing)**이 필요한 경우, 이를 처리  

4. 복제본 동기화  

5. 타임아웃 관리

### cluster.c

### cluster.h
clusterDictionaryMetadata 구조체에서 슬롯에 대한 메타데이터를 저장 및 관리한다.

